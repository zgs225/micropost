<%= form_for current_user.relationships.find_by(followed_id: @user.id), 
    html: { method: :delete, role: 'form' }, remote: true do |f| %>
    <%= f.submit '取消关注', class: 'btn btn-default btn-large' %>
<% end %>
